#schedulerSearch=SELECT C.DELIVERY_COUNT, U.USER_NAME, U.USER_NO, U.COMPANY, U.MANAGER, S.SCHEDULE_DATE FROM USER_INFO U JOIN CONTRACT C ON(U.USER_NO = C.USER_NO) JOIN SCHEDULE S ON(U.USER_NO = S.USER_NO_CONTRACT) WHERE U.MANAGER = ?
schedulerSearch=SELECT DISTINCT C.DELIVERY_COUNT, U.USER_NAME, U.USER_NO, U.COMPANY, U.MANAGER, S.SCHEDULE_DATE  FROM USER_INFO U JOIN CONTRACT C ON(U.USER_NO = C.USER_NO) JOIN SCHEDULE S ON(U.USER_NO = S.USER_NO_CONTRACT) WHERE U.MANAGER = ?
schedulerUpdate=UPDATE SCHEDULE SET SCHEDULE_DATE=? WHERE USER_NO_CONTRACT=?
